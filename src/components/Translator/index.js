import React, { Component } from 'react';
import Fetch_Word from './components/fetch_word';

class Translator extends Component {
  render() {
    return(
      <div>
        <Fetch_Word />
      </div>
    );
  }
}

export default Translator;
